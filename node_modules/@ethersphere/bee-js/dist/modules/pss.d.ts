import WebSocket from 'isomorphic-ws';
import type { BeeResponse, PublicKey } from '../types';
/**
 * Send to recipient or target with Postal Service for Swarm
 *
 * @param url Bee url
 * @param topic Topic name
 * @param target Target message address prefix
 * @param recipient Recipient public key
 *
 */
export declare function send(url: string, topic: string, target: string, data: string | Uint8Array, recipient?: PublicKey): Promise<BeeResponse>;
/**
 * Subscribe for messages on the given topic
 *
 * @param topic Topic name
 */
export declare function subscribe(url: string, topic: string): WebSocket;
