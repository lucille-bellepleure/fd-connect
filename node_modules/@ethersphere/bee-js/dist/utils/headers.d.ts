import { Dictionary, FileHeaders, UploadHeaders, UploadOptions } from '../types';
export declare function readFileHeaders(headers: Dictionary<string>): FileHeaders;
export declare function extractUploadHeaders(options?: UploadOptions): UploadHeaders;
